# Roadmap

<!-- BLUEPRINT: Describe the phases of this project. Keep the spike structure
below for any phase that de-risks 🟡 proposed decisions. -->

Work that de-risks 🟡 proposed decisions before they are promoted to ✅ is
organised as **validation spikes** — each proves one claim or answers one
question, in isolation, as the narrowest slice that forecloses nothing. A
spike is distinct from a **vertical slice / MVP**, which exercises all pillars
end-to-end while deferring every hard choice to its `Q##`.

## Phase 0 — validation spikes

<!-- BLUEPRINT: one section per spike, following this skeleton (replace it
with real spikes — the gate flags this comment until you do). Re-scope a
spike in place with an admonition recording which session removed which
premise — never delete or renumber it:
  !!! abstract "Reframed (Session N)"

### Spike A — <name>

**Answers Q## / Proves or de-risks D-0##.**

- **Scope:** <what is in — the narrowest slice that answers the question>
- **Exit criterion (green):** <the observable pass condition>. Measured but
  not gating: <metrics recorded for later, without blocking>.
- **Explicitly deferred:** <items> (gated on Q##).
- **Related:** <topic pages · ADRs · questions>
-->

## Exit criteria

How spike outcomes flip registry statuses — promotion happens here, not
informally:

<!-- BLUEPRINT: e.g. "Spikes A–B green → promote D-008 and D-009 to ✅;
Spike A red → escalate Q2 and revisit D-008." -->
