"""{{PROJECT_NAME}} — see docs/ (canonical) and CLAUDE.md for orientation.

Subpackages mirror the architecture seams; each stub's docstring points at
its docs page. The seams are the future package/service boundaries — keep
them honest (tests/test_package.py imports every one).
"""

__version__ = "0.0.1"
